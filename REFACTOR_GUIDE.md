# ThreeKingdoms-AI-Venture-Vue Refactor Guide

## 改造目标

这个复制目录不是在原工程上打补丁，而是针对原项目高耦合问题做了一次独立重构，核心目标有四个：

1. 前端只负责显示和交互，不再负责世界逻辑、提示词拼接和长期记忆。
2. 后端统一负责会话状态、时间推进、事件演算、候选行动和流式叙事。
3. 游戏可以在没有外部大模型的情况下继续运行，避免强依赖接口导致整局中断。
4. 解决上下文丢失、重复轮回、时间感缺失和玩法单一的问题。

## 现在的架构

- 前端：`Vue 2 + Element UI`
- 后端：`Node.js` 原生 `http` 服务
- 会话存档：`server/runtime/sessions/*.json`
- 页面入口：`src/views/game/GamePage.vue`
- 后端入口：`server/index.js`

现在的职责划分如下：

- 前端负责：
  - 创建或恢复会话
  - 渲染场景、属性、线索、成就、日志
  - 提交用户动作
  - 接收并实时显示流式文本
  - 保存本地会话 ID 和模型配置
- 后端负责：
  - 初始化剧本、出身、NPC、开放线程
  - 识别动作意图
  - 推进年月、地点、目标和阶段
  - 根据规则修改属性、道具、关系、成就
  - 检测重复动作并强制打破循环
  - 生成候选行动
  - 对接外部兼容 OpenAI 的模型接口
  - 在没有模型时使用本地后备叙事模板

## 这次修复了什么

### 1. 前端高耦合

原本前端同时负责：

- UI 展示
- 大模型接口调用
- 提示词构造
- JSON 修复与解析
- 长期记忆摘要
- 结局生成
- NPC 互动

现在这些内容被移到 `server/game/` 下，前端只保留纯展示和交互。

### 2. 没有真正的状态管理

原项目主要靠组件状态和 `localStorage`。现在每个会话都会在后端落盘，统一保存：

- 角色属性
- 世界状态
- 最近动作
- 大事件摘要
- 悬而未决的线程
- 成就
- 场景信息

### 3. 没有流式接收

原项目需要等完整结果返回才显示。现在使用 NDJSON 流式输出：

- 后端逐段推送状态和文本
- 前端通过 `ReadableStream` 实时拼接
- 用户可以立即看到剧情滚动生成

### 4. 上下文和时间感薄弱

现在每个回合都会推进：

- 年份
- 月份
- 季节
- 阶段
- 地点
- 当前目标

同时后端会记录近期动作和事件摘要，避免大模型每次都像“失忆重开”。

### 5. 重复轮回和玩法单一

后端新增了循环检测和线程压力机制：

- 如果玩家连续对同一类事情原地重复处理，系统会判定为循环
- 一旦触发，世界压力上升，并强制生成新威胁或新线索
- 候选行动不再只依赖模型返回，而是由后端规则先生成一批有方向的选项

### 6. 过度依赖大模型

现在即使没有配置任何外部模型：

- 游戏也可以完整推进
- 文本会使用本地叙事模板流式输出
- 不会因为模型不可用而卡死

## 这次补上的联调细节

- `toSnapshot()` 现在会返回 `pendingThreads`，前端能稳定显示悬而未决线索。
- 模型列表接口始终保留 `local-fallback`，可以随时切回本地叙事。
- 点击“重开局”时保留当前会话的模型设置，不会把配置一起清空。
- 后端入口支持直接 `npm start`，也支持在代码里单独加载做验证。
- 构建配置已切换到新的 `config/refactor.js`，开发代理改为本地 `/api -> http://localhost:8111`。

## 启动方式

要求：

- `Node.js >= 18`
- `npm >= 9`

安装依赖：

```bash
npm install
```

开发模式需要两个终端：

终端 1：

```bash
npm run server
```

终端 2：

```bash
npm run dev
```

开发访问：

- 前端：`http://localhost:9527`
- 后端：`http://localhost:8111`

生产构建：

```bash
npm run build
```

生产运行：

```bash
npm start
```

然后访问：

- `http://localhost:8111`

## 建议测试项

1. 进入页面后是否能自动创建会话并展示出身选择。
2. 选择出身后是否立即开始流式生成剧情。
3. 连续多次执行同类动作时，是否会出现新压力和新线索，而不是一直原地踏步。
4. 点击“重开局”后，模型配置是否仍然保留。
5. 不配置外部模型时是否还能继续游玩。
6. 配置兼容 OpenAI 的模型接口后，是否能正常流式输出。
